-   Make the following changes to the Preferences page.

    -   Remove the `Study Language` dropdown
    -   Remove session nudges section
    -   Remove the Focus snapshot section
    -   Include user's CEFR rating in the profile card.
    -   Remove the gradient background
